using System;
using Rubicon.RegisterNova.Infrastructure.JetBrainsAnnotations;
using Rubicon.RegisterNova.Infrastructure.TestData.ValueGeneration;

namespace Rubicon.RegisterNova.Infrastructure.TestData.ValueChain
{
  public interface IValueProviderTreeNode
  {
    


    /*bool HasValue ();
    object GetValue();

    void SetValueProvider (IValueProvider valueProvider);
    IValueProviderTreeNode SetChainProvider(IValueProvider valueProvider, Type providerType, string nameFilter=null);
    bool HasLink(Type providerType, string nameFilter=null);
    IValueProviderTreeNode GetLink(Type providerType, string nameFilter);

    IValueProviderTreeNode PreviousLink { get; }*/
  }
}